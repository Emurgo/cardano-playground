apiVersion: apps/v1
kind: Deployment
metadata:
  name: cardano-node-local
  namespace: cardano-node-{{ .Values.network }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cardano-node-{{ .Values.network }}
  template:
    metadata:
      labels:
        app: cardano-node-{{ .Values.network }}
    spec:
      containers:
      - name: cardano-node-local
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        env:
        - name: NETWORK
          value: "{{ .Values.network }}"
        - name: PORT
          value: "3001"
        volumeMounts:
          - name: testnodeipc
            mountPath: /root/.local/share/cardano-playground.git
      volumes:
        - name: testnodeipc
          persistentVolumeClaim:
            claimName: ipc
      imagePullSecrets:
        - name: testcred
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: cardano-node-local
  namespace: cardano-node-{{ .Values.network }}
spec:
  selector:
    app: cardano-node-local
  ports:
  - name: http
    port: 3001
    targetPort: 3001
  type: NodePort
